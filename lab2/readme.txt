Running terminal in the same folder with the branchsimulator.cpp file, Compilie the code with following command:
g++ branchsimulator.cpp -o branchsimulator.out
Then, execute the simulator with the following command:
./branchsimulator.out config.txt trace.txt
